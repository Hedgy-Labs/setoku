node_modules/
plugin/node_modules/
*.log
.DS_Store
license-report.tsv

# local embedding-model cache (fastembed downloads; never committed)
local_cache/
.fastembed/
.env
backups/

# box deploy target (per-machine, never committed/on-box)
deploy/target.local
