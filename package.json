{
  "name": "setoku-dev",
  "private": true,
  "description": "Dev/test harness for the Setoku plugin (the installable plugin lives in ./plugin)",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "test": "bun test test/ ingest/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "pg": "^8.16.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "@types/pg": "^8.15.0",
    "typescript": "^5.8.0"
  }
}
