{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"${CLAUDE_PLUGIN_ROOT}\" && (bun --silent -e \"await import('@modelcontextprotocol/sdk/server/mcp.js'); await import('pg'); await import('zod');\" >/dev/null 2>&1 || (rm -rf node_modules bun.lock && bun install --silent --no-progress >/dev/null 2>&1)) || true"
          }
        ]
      }
    ]
  }
}
